{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/mobile exec expo export --platform web",
  "outputDirectory": "artifacts/mobile/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
