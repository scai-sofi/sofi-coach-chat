// Re-export shared persona goals from the main constants directory.
// Phase2 uses the same goal definitions to ensure consistency across both app versions.
export {
  MAYA_GOALS,
  MARCUS_GOALS,
  JORDAN_GOALS,
  DAVID_GOALS,
  PERSONA_GOALS,
} from '../../constants/personaGoals';
